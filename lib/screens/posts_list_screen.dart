import 'package:flutter/material.dart';

class PostsListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Posts List'),
      ),
      body: Center(
        child: Text('Posts List Screen'),
      ),
    );
  }
}
